# Configuración de SSH y GitHub

## Generación de llaves
```bash
ssh-keygen -t rsa -b 4096 -C "eduardo.sebas.acuna.lujan@gmail.com" -f /c/Trabajos/Seguridad/id_rsa


git add .
git add .

git commit -m "Subir archivos"
git push
```
	